

const pronombre = ['the', 'our'];
const adjetivo = ['great', 'big'];
const nombre = ['jogger', 'racoon'];
const dominio = [".com", ".net",".es", ".eu"];

for (let i = 0; i < pronombre.length; i++) {
for (let a = 0; a < adjetivo.length; a++) {
  for (let b = 0; b < nombre.length; b++) {
    for (let c = 0; c < dominio.length; c++) {
      console.log(pronombre[i]+ adjetivo[a] 
  + nombre[b] + dominio[c]);
    }
    
  }
  
}

}
